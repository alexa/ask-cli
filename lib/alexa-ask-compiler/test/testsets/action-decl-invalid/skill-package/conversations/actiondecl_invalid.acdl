namespace test.invalidActionDeclaration

import slotTypes.CityName
import com.amazon.alexa.schema.Nothing

// Action with arguments with default expression Nothing
// Only nothing is supported as defaultExpression today
// BUG - error message is not returned when other statements fail below
action Nothing action_with_arguments_default_expression(CityName cityName = "hello")

// Action declarations are not allowed to contain generics today
action Nothing action_with_generics<T>(T arg)
