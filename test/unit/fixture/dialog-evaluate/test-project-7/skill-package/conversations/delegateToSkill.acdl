namespace delegateToSkill

import com.amazon.alexa.ask.conversations.Invoke
import com.amazon.alexa.ask.conversations.Notify
import com.amazon.alexa.ask.conversations.expect
import com.amazon.alexa.ask.conversations.response
import com.amazon.alexa.schema.Nothing
import com.weatherbot.apis.secondEarthAPI
import com.weatherbot.utteracesets.delegateToSkill
import prompts.secondEarthAPI_apla
import slotTypes.*
import prompts.*
import displays.*
import com.weatherbot.utteracesets.*
import com.amazon.alexa.ask.conversations.*
import com.amazon.alexa.schema.*
import types.*
dialog Nothing testTwo() {
  sample {
    expect(
      act = Invoke,
      event = delegateToSkill
    )
    secondEarthAPI()
    response(
      response = secondEarthAPI_apla,
      act = Notify {
        actionName = secondEarthAPI
      },
      nextAct = nothing
    )
  }
}
